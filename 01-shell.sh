#!/bin/bash

# #! is called as she-bang, it denotes the path of interpreter and also ensure the script executes with that interpreter if you execute the script with ,/script.sh instead of bash script.sh

# Any line starts with # is a comment and those lines will be ignored during execution
