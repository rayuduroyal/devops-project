- name: Demo on loops
  hosts: localhost
  vars:
    FRUITS: [ "apple", "banana", "orange"]
  tasks:
    - name: Print All fruits
      debug:
        msg: "Fruit Name = {{item}}"
      loop: "{{FRUITS}}"

